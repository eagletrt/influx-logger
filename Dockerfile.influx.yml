FROM influxdb:2

EXPOSE 8086
ENV DOCKER_INFLUXDB_INIT_MODE=setup
ENV DOCKER_INFLUXDB_INIT_USERNAME=test_admin
ENV DOCKER_INFLUXDB_INIT_PASSWORD=admin_password
ENV DOCKER_INFLUXDB_INIT_ORG=eagletrt
ENV DOCKER_INFLUXDB_INIT_BUCKET=telemetry
ENV DOCKER_INFLUXDB_INIT_TOKEN=influxdb-token
